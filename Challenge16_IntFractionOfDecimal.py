from math import trunc

n = float(input("Type a decimal number: "))

print("The integer part of {} is {}.".format(n, trunc(n))) #Explain different between Trunc, Floor, Ceil and Round / or cast directly with int()